<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
		{% endblock %}
    </head>
    <body>
        <div class="container">
            <nav class="navbar navbar-inverse">
                <div class="container-fluid">
                    <ul class="nav navbar-nav">
                        <li> <a href="{{ path('homepage') }}">Home</a> </li>
                        <li> <a href="{{ path('inscription') }}">Sign Up</a> </li>
                        {% if app.user %}
                            <li><a href="{{ path('security_logout') }}">Logout</a></li>
                            <li> <a href="{{ path('profil', {'url' : app.user.username}) }}">Profil</a> </li>
                        {% else %}
                            <li><a href="{{ path('security_login') }}">Login</a></li>
                        {% endif %}
                        <li> <a href="{{ path('take_date') }}">Réserver une place</a> </li>
                        <li> <a href="{{ path('make_date') }}">Proposer une réservation</a> </li>
                        <li> <a href="{{ path('add_img') }}">Ajouter un image</a> </li>
                    </ul>
                </div>
            </nav>
        </div>
        {% block body %}
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
